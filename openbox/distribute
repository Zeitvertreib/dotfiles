DIR="$( cd "$( dirname "${BASH_SOURCE}" )" && pwd )"
RC="$HOME/.config/openbox/rc.xml"
now=$(date +"%m_%d_%Y")

if [ -f $RC ] && [ ! -L $RC ]; then
    mv $RC ${RC}_$now
    ln -s $DIR/rc.xml $RC
fi

function openbox_revert {
    echo 1
}
