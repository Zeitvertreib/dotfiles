RC="$HOME/.config/openbox/rc.xml"
now=$(date +"%m_%d_%Y")

if [ -f $RC ] && [ ! -L $RC ]; then
    mv $RC ${RC}_$now
    ln -s $DIR/openbox/rc.xml $RC
    echo "openbox: settings overwritten"
fi

function openbox_revert {
    echo 1
}
